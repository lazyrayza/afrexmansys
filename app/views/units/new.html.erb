<h2>Sign up</h2>

<%= simple_form_for ([@unit, @development])  do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :unit_number,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "number" }%>
    <%= f.input :floor,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "floor" } %>
    <%= f.input :size,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "size" } %>
    <%= f.input :bedrooms,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "size" } %>
    <%= f.input :bathrooms,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "size" } %>
    <%= f.input :price,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "size" } %>
    <%= f.input :apartment,
                required: true,
                autofocus: true,
                collection: [["Apartment", true],["House", false]],
                input_html: { multiple: false, class: "multiple-select" } %>
    <%= f.input :description,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "Description" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up" %>
    <%# raise %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
