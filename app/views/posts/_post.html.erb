<div class="post <%= user_is_posts_author ? 'right' : '' %>">
              <%= cl_image_tag(post.tenant.photo.url, class: 'avatar')%>
  <div class="post-content"><%= post.description %></div>
</div>
